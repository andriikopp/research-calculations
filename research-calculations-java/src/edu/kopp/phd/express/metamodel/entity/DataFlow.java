package edu.kopp.phd.express.metamodel.entity;

public class DataFlow extends Node {

    public DataFlow(String label, int fromActivity, int toActivity) {
        super(label, fromActivity, toActivity);
    }
}
