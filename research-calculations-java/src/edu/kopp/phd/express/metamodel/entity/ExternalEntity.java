package edu.kopp.phd.express.metamodel.entity;

public class ExternalEntity extends Node {

    public ExternalEntity(String label, int preceding, int subsequent) {
        super(label, preceding, subsequent);
    }
}
